# TURBO MVP Configuration
PORT=3000
NODE_ENV=development

# Database (SQLite file path)
DATABASE_PATH=./knowledge.db

# Optional: Gemini CLI for advanced entity extraction
# Leave empty to use only local regex extraction
GEMINI_CLI_ENABLED=false

# Debug mode (set to true for verbose logging)
DEBUG=false
DEBUG_DB=false
